name: 'Generate nonprod changelog'
description: 'Generate nonprod changelog'

outputs:
  changelog:
    description: 'The generated changelog'
    value: ${{ steps.create-nonprod-changelog.outputs.changelog }}

runs:
  using: 'composite'
  steps:
    - name: "Env variable: General"
      shell: bash
      env:
        REPOSITORY_NAME: ${{ github.event.repository.name }}
      run: echo "REPO_BASE_URL=https://github.com/CleverShuttle/$REPOSITORY_NAME" >> $GITHUB_ENV
    - name: "Checkout repository"
      uses: actions/checkout@v3
    - name: 'Generate changelog'
      id: create-nonprod-changelog
      shell: bash
      run: |
        set -o nounset -o errexit
        # Escape / in url
        escapedBaseUrl=$(sed -e "s/\//\\\\\//g" <<< ${REPO_BASE_URL} )
        output=$(git log --merges origin/master..origin/nonprod \
          | grep 'into nonprod' \
          | sed -rn "s/^\s*Merge remote-tracking branch 'origin\/(.*)' into nonprod/[\1](${escapedBaseUrl}\/tree\/\1)/p" \
          | sort | uniq)
        # These replacements are somehow necessary for the multiline changelog to arrive in the last step ...
        output="${output//'%'/'%25'}"
        output="${output//$'\n'/'%0A'}"
        output="${output//$'\r'/'%0D'}"
        echo "::set-output name=changelog::$output"

    - name: 'Outputs'
      shell: bash
      env:
        CHANGELOG: ${{ steps.create-nonprod-changelog.outputs.changelog }}
      run: |
        echo "Changelog: '$CHANGELOG'"
